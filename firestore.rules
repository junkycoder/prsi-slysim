rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId}/games/{gameId} {
      // allow create if do cument not exists already
      allow create: if exists(document) == false;
    }

  }
}